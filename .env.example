# Admin Dashboard Password
VITE_ADMIN_PASSWORD=sonnyvan2026

# Note: This is an example file. Copy this to .env (without .example)
# and set your actual password. The .env file is in .gitignore so it
# won't be committed to version control.
